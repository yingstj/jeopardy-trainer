# Rename this file to secrets.toml and fill in your credentials

# For Google OAuth (optional - if not set, will use simple email/password login)
# To get these:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Create credentials (OAuth 2.0 Client ID)
# 5. Add http://localhost:8501 to Authorized redirect URIs

GOOGLE_CLIENT_ID = ""
GOOGLE_CLIENT_SECRET = ""
REDIRECT_URI = "http://localhost:8501"

# For deployed version, change REDIRECT_URI to your deployed URL
# REDIRECT_URI = "https://your-app.streamlit.app"